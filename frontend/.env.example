# Optional MapLibre style URL. Without this, SaferPath uses its isolated
# low-noise local fallback style so fixture routes still render in development.
VITE_MAP_STYLE_URL=https://example.com/your-map-style.json
# Backend API prefix. Defaults to /v1 for a same-origin deployment.
VITE_API_URL=/v1
